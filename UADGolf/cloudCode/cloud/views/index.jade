extends layout

block content
    div#project-info
        h3#title Up and Down Golf
        p#description This page is used to run the application, assuming the user has configured the Wiced Sense sensor correctly.  Upon the click of the "Connect to Wiced Sense" button, the user may begin his or her putting motion, and upon completion, the motion may be visualized on the screen below.
        button(type='button', id='startButton') Connect to Wiced Sense
        br
        br
        canvas(id="puttCanvas", width="", height="")
        //each frame in frames
        //p #{frame[0]} #{frame[1]} #{frame[2]}
    
    


                    
